# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
DB_HOST = localhost
DB_PORT = 3306
DB_USER = root
DB_PASSWORD = 你的密码
DB_NAME = resume_screening
# Authentication (Optional - can be configured in System Settings)
# AUTH_SECRET=your_auth_secret

# Database (if applicable)
# DATABASE_URL=postgresql://user:password@localhost:5432/resume_screening
# JWT 密钥（生产环境请修改为安全的随机字符串）
JWT_SECRET= 你的密码

# 可选：邀请码
# INVITE_CODE=xxxxxxxxxx

# DashScope AI（简历解析）
# 请在这里填入你的 DashScope 兼容模式应用的 API Key（示例值请自行替换）
DASHSCOPE_API_KEY=your_dashscope_api_key
# 可选：如果不填，将使用默认兼容模式地址
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/api/v2/apps/protocols/compatible-mode/v1
# 可选：大模型名称，默认 qwen3.5-plus，可按实际模型调整
DASHSCOPE_MODEL=qwen3.5-plus
